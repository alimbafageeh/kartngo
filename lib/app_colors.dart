import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF5FAFD);  // app bg
  static const primary = Color(0xFF2A92CF);     // buttons, cart bar
  static const accent = Color(0xFFAAE6FF);      // chip active bg
  static const chipInactive = Color(0xFFF2F7FA);
  static const chipBorder = Color(0xFFD6E8F1);
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF6B6B6B);
  static const price = Color(0xFF3A97D3);
  static const white = Color(0xFFFFFFFF);
  static const shadow = Color(0x1A000000); // black with 10% opacity
}
